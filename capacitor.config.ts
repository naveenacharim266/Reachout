import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'singlepage-prep',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
